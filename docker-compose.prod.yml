services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner   # optimised production image
    volumes:
      - db_data:/data
    command: sh -c "npx prisma db push && node server.js"
    restart: unless-stopped

volumes:
  db_data:
